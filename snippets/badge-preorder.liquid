{% comment %}
    Renders "Pre-order" badge for product.

    Accepts:
    - size: {String}
{% endcomment %}

{% liquid
    assign text = settings.badge_preorder__text | escape
    assign bgColor = settings.colors_badge_preorder__background_gradient | default: settings.colors_badge_preorder__background_color
    assign textColor = settings.colors_badge_preorder__foreground_color
    assign position = settings.badge_preorder__position
    assign border_radius = settings.badges_border_radius_value | append: 'rem'

    assign color = 'color:' | append: textColor | append: ';background:' | append: bgColor
    if settings.check_theme_style == 'light'
        assign color = 'color:' | append: bgColor | append: ';background:' | append: textColor
    endif
%}

{% unless text == blank or position == 'none' %}
    <div
        class="badge badge-size-{{ size }} badge-preorder {{ position }}"
        style="border-radius:{{ border_radius }};{{ color }}"
        aria-hidden="true"
    >
        {{ text }}
    </div>
{% endunless %}
